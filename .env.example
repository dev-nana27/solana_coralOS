# Copy to .env and fill in. Never commit .env — it is gitignored.

# ── Wallets (node scripts/setup.js generates these) ───────────────────────────

# Buyer keypair (base58 secret) — signs the escrow deposit/release and the TxLINE subscribe.
# Fund it at https://faucet.solana.com (GitHub sign-in; CLI/RPC airdrops are gated).
BUYER_KEYPAIR_B58=

# Seller receive wallet (public key) — the escrow pays this on release. Defaults to the buyer
# (self-pay) if unset, so one funded wallet is enough; set it to pay a distinct seller.
WALLET=

SOLANA_RPC_URL=https://api.devnet.solana.com

# ── LLM brain (the agent's value call) ────────────────────────────────────────

# Anthropic by default:
ANTHROPIC_API_KEY=

# …or flip to OpenAI (e.g. a sponsored key) — no code change:
#   LLM_PROVIDER=openai
OPENAI_API_KEY=
LLM_PROVIDER=
LLM_MODEL=

# ── TxLINE (optional) ─────────────────────────────────────────────────────────
# The proxy subscribes the buyer wallet to the free World Cup tier on its own, so you don't need to
# set these. `npm run mint` (examples/txodds) writes TXLINE_API_KEY here for the standalone TxLineClient.
TXLINE_API_KEY=
TXLINE_BASE_URL=
